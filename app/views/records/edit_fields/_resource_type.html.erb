<%= f.input :resource_type, as: :select_with_help, collection: Rails.configuration.resource_types,
    input_html: { class: 'form-control', multiple: true, size: Rails.configuration.resource_types.size} %>
