<% # TODO: watch for this fix upstream %>
<%= f.input :rights, as: :select_with_help, collection: Rails.configuration.cc_licenses,
  input_html: { class: 'form-control', multiple: false, :name => 'generic_work[rights][]'}
%>
